# 申请开票页面自测清单

## 我的订单

- [x] 正常已支付会员卡、门票和补差价订单显示“申请开票”。
- [x] `paid_dup`、退款中、已退款订单不显示申请入口。
- [x] 已提交/审核中显示“查看开票进度”，已开票显示“查看发票”。
- [x] 被驳回申请显示“修改并重新申请”。
- [x] 已开票门票订单不再显示自助退款入口。
- [x] 开票操作携带订单号进入对应申请页或详情页。
- [x] 新增操作与继续支付、申请退款可以在窄屏换行，不覆盖金额。
- [ ] 微信开发者工具真机尺寸检查：iPhone SE / 常规 iPhone / 大字体模式。

> 后续页面完成后继续在本文件追加对应自测清单。

## 申请开票

- [x] 页面只展示服务端返回的订单名称、订单号和可开票金额。
- [x] 默认个人抬头，切换单位后显示税号与更多单位信息。
- [x] 切回个人抬头会清理税号、地址、电话和银行资料。
- [x] 单位选填资料默认折叠，展开动画只使用透明度和位移。
- [x] 个人与单位必填项、邮箱、电话和银行账号均有本地校验。
- [x] 未同意隐私政策时不能提交。
- [x] 提交前二次确认抬头、金额和邮箱。
- [x] 确认与提交阶段均有防重复锁，失败保留表单内容。
- [x] 驳回申请可以回填资料及原因后重新提交。
- [x] 固定底栏适配底部安全区，颜色、圆角与阴影只使用全局 Token。
- [ ] 微信开发者工具检查输入法弹起、长单位名称、大字体和邮箱键盘体验。

## 开票进度

- [x] 已提交、审核中、已开票正确映射到三段进度。
- [x] 统一展示预计 10 个自然日内完成，不显示误导性的实时倒计时。
- [x] 已开票展示 PDF 文件名、大小、版本和开具时间，不承诺邮件已发送。
- [x] “查看电子发票”每次重新获取授权临时地址，下载后使用 PDF 文档能力打开。
- [x] “下载电子发票”写入下载用途审计，并明确提示可从文档右上角转发或保存。
- [x] 文件操作期间锁定重复点击，临时地址过期、下载失败或打开失败均可再次重试。
- [x] 新 PDF 申请不下发云文件 ID；历史 `https://` / `cloud://` 发票地址保留兼容查看入口。
- [x] 已开票但文件暂不可用时展示明确说明和“重新加载”，不显示失效按钮。
- [x] 驳回状态显示具体原因并可返回修改资料。
- [x] 因退款关闭时提供明确状态说明，不显示可继续处理的假入口。
- [x] 下拉刷新重新读取服务端状态，失败显示可重试错误态。
- [x] 订单金额、订单号和申请时间使用等宽数字。
- [x] 状态不只依赖颜色，同时有文字、节点和说明。
- [x] 页面颜色、圆角、阴影和动效只使用项目 Token。
- [x] 自动化覆盖新 PDF 查看、下载失败重试、历史地址兼容和无文件恢复。
- [ ] 微信开发者工具检查真实 PDF、临时地址过期、超长文件名、动态字体和下拉刷新反馈。

## 员工开票列表

- [x] 员工模式入口仅对管理员显示“开票管理”。
- [x] 普通接待员工直接进入页面时不加载任何开票资料。
- [x] 默认展示全部申请，可按待审核、审核中、已开票和已驳回筛选。
- [x] 切换筛选条件后重新请求服务端数据，不复用上一状态结果。
- [x] 列表展示抬头类型、抬头名称、订单、金额、脱敏邮箱和申请状态。
- [x] 点击整张申请卡片进入对应处理详情。
- [x] 加载失败提供重试入口，无数据时显示对应空状态。
- [x] 页面颜色、圆角、阴影和状态样式只使用项目 Token。
- [ ] 微信开发者工具检查长抬头、长订单号、小屏横向筛选和下拉刷新体验。

## 员工开票详情

- [x] 页面通过管理员专用云端接口读取完整开票资料，普通员工无法越权获取。
- [x] 明示税号、地址、电话和银行资料仅用于发票开具。
- [x] 待审核申请可以进入审核中，申请人进度页同步显示状态变化。
- [x] 完成开票改为“选择 PDF → 上传并绑定申请 → 确认交付”，不再手填发票号码、文件地址或勾选邮件已发送。
- [x] 每次只允许选择 1 个 `.pdf` 文件，客户端同时检查扩展名、10MB 上限和 `%PDF-` 文件头。
- [x] 服务端重新检查文件类型、实际大小、PDF 文件头、申请目录和申请版本，不能只信任客户端元数据。
- [x] 上传路径固定归属当前申请；绑定时校验管理员权限和申请版本，申请资料变更后旧文件不能用于完成开票。
- [x] 上传成功显示文件名、大小和版本，可预览；完成开票前可以替换，替换成功后清理旧云文件。
- [x] 上传文件本身不改变开票状态；只有管理员再次确认后才进入“已开票”。
- [x] 完成开票要求当前申请已绑定有效 PDF，重复提交同一版本按幂等成功处理。
- [x] PDF 临时访问地址必须经过申请人/管理员鉴权，申请详情不下发云文件 ID 和管理员身份。
- [x] 每次预览或下载临时地址都原子写入访问审计记录，不因预览改变申请排序时间。
- [x] 驳回申请必须填写具体原因，失败时保留已输入内容。
- [x] 选择、上传、预览和确认期间分别锁定操作，避免重复上传、重复开票或重复驳回。
- [x] `chooseMessageFile` 因未声明文件权限失败时，弹窗明确提示需在微信后台隐私保护指引声明“选中的文件”。
- [x] 员工开票详情挂载隐私授权弹层，平台要求授权时可按微信标准流程继续。
- [x] 已开票与已驳回状态改为只读结果卡，不再展示失效操作。
- [x] 敏感长文本支持换行，金额、订单号、税号和账号使用等宽数字。
- [x] 页面颜色、圆角、阴影和动效只使用项目 Token，并适配底部安全区。
- [x] 自动化覆盖合法 PDF、伪装 PDF、超限 PDF、过期申请版本、替换文件、预览和完成开票。
- [ ] 微信开发者工具检查真实 PDF、网络失败、替换文件、重复点击、大字体模式和 10MB 边界。

## 隐私政策与退款联动

- [x] 隐私政策列明发票抬头、单位税号、接收邮箱和选填银行资料。
- [x] 隐私政策列明管理员处理开票时会选择并上传电子发票 PDF 文件。
- [x] 明确开票资料仅用于核对订单、开具和发送电子发票。
- [x] 明确完整资料仅限已审批管理员在处理发票时访问。
- [x] 已开票订单在会员卡与门票退款云函数中均被服务端阻止。
- [x] 阻止退款时明确提示联系工作人员先处理红字发票。
- [x] 待审核和审核中的申请在自动退款或人工退款受理后自动关闭。
- [x] 退款关闭保留系统操作历史，订单摘要同步为 `cancelled_refund`。
- [x] 管理员完成开票前再次检查订单退款状态，防止并发误开。
- [x] 部分退款后重新申请会读取当前票券并重新计算可开票金额。
- [ ] 云开发测试环境完成“申请中退款”和“已开票退款”两条真机联调。
- [x] 微信公众平台“用户隐私保护指引”中的“选中的文件”声明已审核通过（业主 2026-07-29 确认）。
- [ ] 在包含最新代码的体验版/正式版中，真机选择聊天中的 PDF，完成上传、预览、绑定和开票确认。

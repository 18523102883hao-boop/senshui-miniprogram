# 正式上线检查报告（2026-07-30）

## 结论

当前版本可作为 **GitHub 候选发布版本** 提交评审，但在完成下方“正式发布前必做项”之前，不建议直接提交微信正式版审核或发布。

代码完整性、自动化测试、微信开发者工具预览打包、主包体积、页面注册、云函数源码完整性、JSON/JavaScript 语法和敏感信息扫描均已通过。剩余风险主要集中在真实账号/真实支付链路回归、云端权限与索引确认，以及第三方云开发 SDK 的上游依赖告警。

## 已通过的发布闸门

- Node 自动化测试：`555` 项通过，`0` 失败。
- JavaScript / MJS 语法检查：全部通过。
- JSON 配置解析：`137` 个文件全部通过。
- 页面完整性：`51` 个页面均具备 `.js`、`.json`、`.wxml`、`.wxss` 文件。
- 云函数清单：默认部署清单中的 `32` 个云函数均具备入口文件和依赖配置。
- 微信开发者工具预览打包：成功。
- 小程序主包：`1525 KB`，低于微信小程序 `2 MB` 主包上限。
- `git diff --check`：通过。
- 敏感词与已知密钥扫描：未发现 AppSecret、CLI 令牌或私钥进入工作区及 Git 历史。
- 小程序前端生产依赖审计：`0` 个已知漏洞。
- `adminOperationsLedger` 云函数的 `wx-server-sdk` 已由不确定的 `latest` 固定为 `4.0.2`，保证部署可复现。

## 已确认但不阻断候选版本提交的提示

- 开发者工具预览给出 `5` 条编译提示，均为已明确排除的 source map、npm 元数据或无依赖文件，不是编译错误。
- `adminOperationsLedger` 依赖审计报告包含 `14` 个高危和 `2` 个中危告警，来源为 `wx-server-sdk`、`@cloudbase/node-sdk` 和 `exceljs` 的上游传递依赖。
- 当前业务代码未向这些依赖传入用户可控 URL、代理配置、文件路径模式或对象属性路径；直接可利用性较低，但上游尚未提供无破坏性升级路径。`npm audit fix --force` 会降级核心 SDK/Excel 库，因此本次未执行。
- 上述依赖风险应持续跟踪腾讯云开发 SDK 和 ExcelJS 的正式升级，不应通过强制跨大版本覆盖来“消除”报告。

## 正式发布前必做项

1. 在体验版/真机完成真实支付、取消待支付、退款、门票核销、会员核销和补差价收款全链路回归。
2. 使用真实已完成订单完成发票申请、管理员上传 PDF、用户预览和已开票退款限制验证。
3. 分别使用普通员工、检票员工、酒吧员工和管理员账号验证员工入口、角色权限及越权拦截。
4. 在云开发控制台确认运营报表索引已建立，并再次确认 `invoice_requests` 集合和发票文件存储权限未公开。
5. 抽样核对同一日期范围内的订单收入、已核销实际净收入、核销票数、实际人数和退款金额，确保与原始订单/票券记录一致。
6. 真机检查投诉建议图片上传、保险二维码预览、PDF 选择、iPhone 小屏和大字体布局。
7. 核对正式环境云函数版本与本候选提交一致，再生成体验版进行最终验收。

## 发布后观察项

- 云函数错误率、超时率和 `-504002` 等数据库错误。
- 支付回调与取消订单竞态。
- 发票 PDF 上传失败率、临时地址获取失败率。
- 运营报表大日期范围查询耗时、5000 条截断提示和 Excel 导出成功率。
- 核销票数与实际人数差异、历史未知票种待核对数量。

# 员工运营业务账页面自测清单

## 运营业务账汇总

- [x] 页面仅通过管理员接口读取数据，接口返回 `403` 时显示无权限状态，不展示任何账务信息。
- [x] 经营首页固定查询当天自然日，以“今日经营概览”作为管理员进入后第一层信息。
- [x] 页面明确标注“小程序业务账”，并区分“已核销履约口径”和“实际支付/退款资金口径”。
- [x] 经营首页分开展示已支付未核销的订单收入、核销后确认的实际净收入；资金账单继续保留支付金额、实际退款与异常核对。
- [x] “实际入园人数”和“核销票数”分开展示，历史数据缺少人数快照时显示待核对票数，不擅自推算。
- [x] 财务对账和核销数据作为两个一级入口；会员核销、长河令兑换保留为次级业务入口。
- [x] 所有入口携带当天查询范围下钻，避免下级页面统计日期与首页不一致。
- [x] 加载、失败重试、无权限、无数据和大数据截断状态均有独立反馈。
- [x] 页面颜色、圆角、间距、阴影和底部安全区均使用全局 Design Token。
- [x] Node 自动化测试覆盖当天边界、权限、金额格式、风险提示、下钻参数、Design Token 和页面注册。
- [ ] 微信开发者工具接入测试环境，检查管理员真实数据、无数据日期和历史人数快照缺失状态。
- [ ] 真机检查 iPhone SE、常规 iPhone、大字体模式和下拉刷新布局。
- [ ] 云开发部署后复核大数据范围的查询耗时、截断提示以及 5000 条读取上限。

## 财务对账首页

- [x] 默认查询当天，并可继承经营首页传入的起止时间，所有时间范围均使用结束时间开区间。
- [x] 实际净收入按已核销票面金额展示，关联订单按含核销记录的订单号去重，核销票数按票券张数统计。
- [x] 页面明确说明“小程序业务账不等同于微信商户结算账”，不展示到账余额或结算日期等尚无可靠数据的字段。
- [x] 待核对人数和异常核销仅在实际存在时显示风险入口，避免正常经营日长期出现警告。
- [x] 收入构成按已核销票种汇总票数和票面金额，并可下钻到核销明细；“查看全部账单”继续进入资金账单对账页。
- [x] 加载、失败重试、无权限、无数据和大数据截断均有独立状态。
- [x] 页面颜色、圆角、间距、阴影和底部安全区均使用全局 Design Token。
- [x] Node 自动化测试覆盖范围继承、金额格式、风险显隐、下钻参数、权限、异常状态和页面注册，共 7 项。
- [ ] 微信开发者工具以管理员身份验证今日核销收入、去重订单数和票种构成与核销记录一致。
- [ ] 真机检查小屏、大字体、无风险日和有风险日的卡片高度及长收入分类名称。

## 账单对账

- [x] 默认显示截至当前日期的最近 31 个日账单，并支持切换最近 12 个月的月账单。
- [x] 自定义汇总校验日期完整性、起止顺序和 366 天上限，结束日期按次日开区间查询。
- [x] 历史周期按时间倒序排列，每条同时展示订单收入、实际净收入、支付金额、退款金额、资金流水净额、支付/退款订单、核销票数和实际人数。
- [x] 风险数量与历史人数快照缺失分开展示，不把“待核对票数”算成精确人数。
- [x] 默认隐藏空账单，可手动关闭过滤查看零数据的自定义汇总。
- [x] 每条账单携带自身起止时间进入账单详情，避免使用当前筛选范围造成下钻错期。
- [x] 加载、失败重试、无权限、空数据和大数据截断状态均有独立反馈。
- [x] 页面样式仅使用全局 Design Token，并适配底部安全区。
- [x] Node 自动化测试覆盖 31 日范围、12 月范围、排序、风险、票数/人数、日期校验、空账单、下钻和权限，共 7 项。
- [ ] 微信开发者工具抽样核对日、月、自定义汇总与同范围订单和核销事实一致。
- [ ] 真机检查日期选择器、小屏两列指标、大字体以及多条历史账单滚动性能。

## 账单详情

- [x] 页面分为“账单统计”和“账单明细”两个标签，并继承所选账单的精确起止时间。
- [x] 账单统计分开展示订单收入、实际净收入、支付金额、实际退款、资金流水净额、支付/退款笔数、实际净收入构成和异常事项。
- [x] 账单明细支持全部收支、支付、退款切换，并支持订单类型、经办员工和关键词组合筛选。
- [x] 支付/退款筛选在服务端先过滤后分页，避免只过滤当前页导致总数和翻页错误。
- [x] 明细每页 30 条并防止重复翻页；异常和资金明细均可携带订单号进入管理员订单详情。
- [x] CSV 按当前全部筛选条件分页导出，最多 5000 条；不包含完整 OpenID，手机号仅保留后四位。
- [x] 加载、失败重试、无权限、空结果、继续加载和大数据截断均有独立反馈。
- [x] 页面颜色、圆角、间距、阴影和底部安全区均使用全局 Design Token。
- [x] Node 自动化测试覆盖统计、筛选、服务端过滤、分页、导出、隐私、下钻、权限和页面注册；本页与领域契约合计 22 项通过。
- [ ] 微信开发者工具核对支付/退款筛选后的总数、翻页结果和异常订单下钻。
- [ ] 真机验证 350ms 搜索防抖、长订单标题、小屏金额布局，以及 CSV 粘贴到 Excel/WPS 的编码和列结构。

## 核销数据

- [x] 默认查询当天，并支持按日、按月和最长 366 天的自定义范围查询历史记录。
- [x] “核销票数”和“实际人数”作为两个独立主指标展示，待核对票数不计入已确认人数。
- [x] 同时展示关联订单数和实际净收入，并明确实际净收入就是已核销票面金额，不属于当日支付流水。
- [x] 页面分为“验证统计”和“验证明细”；票种统计展示票数、人数、实际净收入和实际人数占比。
- [x] 双人票在统计和明细中均明确显示“1 张 / 2 人”，未知历史票显示“实际人数待核对”。
- [x] 已知单人/儿童票即使旧记录带有未知标记，也按每张 1 人回填；双人票按每张 2 人回填，真正未知票种才保留待核对。
- [x] 明细支持员工、票种和订单号/票号/手机尾号组合筛选，列表按 30 条分页。
- [x] 有关联订单的核销明细可携带订单号进入管理员订单详情；历史无关联订单保持只读展示。
- [x] 加载、失败重试、无权限、空数据、继续加载和大数据截断状态均有独立反馈。
- [x] 页面颜色、圆角、间距、阴影和底部安全区均使用全局 Design Token。
- [x] Node 自动化测试覆盖范围、双口径、双人票、筛选、分页、下钻、权限和页面注册，共 6 项。
- [ ] 微信开发者工具抽样核对单人票、双人票、历史未知票的票数、人数和票面金额。
- [ ] 真机检查日/月/自定义日期控件、长票种名称、小屏指标布局和连续翻页体验。

## 管理员订单详情

- [x] 页面按订单号只读加载订单、支付、退款、票券核销、开票和操作历史，不提供跨业务状态修改入口。
- [x] “购买数量”和“实际人数”分别展示；票券明细继续区分票数与入园人数。
- [x] 会员卡、门票、补差价和历史订单在字段缺失时统一显示“未记录”，不会因空字段中断渲染。
- [x] 退款申请、票券退款和整单退款按事实合并为退款摘要，并保留对应时间线。
- [x] 开票摘要展示申请状态、可开票金额、发票文件和开具时间，敏感抬头资料不进入操作历史。
- [x] 联系方式仅显示手机尾号，页面明确提示隐私口径；服务端不返回完整用户 OpenID。
- [x] 无权限、订单不存在、请求失败、空票券和空历史均有独立反馈。
- [x] 页面颜色、圆角、间距、阴影均使用全局 Design Token。
- [x] Node 自动化测试覆盖订单金额、双口径、退款、核销、开票、历史字段兼容、只读约束和页面注册，共 5 项。
- [ ] 微信开发者工具分别从资金明细、核销明细和异常账单下钻，核对订单号与数据范围。
- [ ] 真机检查长订单号、多个票券、多段时间线、小屏布局和大字体可读性。

## 业务明细

- [x] 从汇总页进入时继承资金、门票核销、会员核销或长河令兑换类别及当前日期范围。
- [x] 支持切换四类业务明细，并按经办员工、业务子类型和关键词组合筛选。
- [x] 关键词连续输入采用 350ms 防抖，确认搜索和清空筛选可立即刷新。
- [x] 列表按 30 条分页加载，滚动到底只发起一次下一页请求并避免重复数据。
- [x] 不同业务类别分别呈现金额、票面金额、核销人数或长河令数量，不混淆财务口径。
- [x] 旧资金流水和门票核销链接继续兼容展示，并携带原日期范围进入新版“账单详情”或“核销数据”。
- [x] 会员核销和长河令兑换继续留在原明细页查询、筛选与导出，不因新版专属页面丢失功能。
- [x] 页面不展示完整员工 OpenID，手机号只显示后四位。
- [x] CSV 导出沿用当前筛选条件，按 100 条分页读取并限制最多 5000 条；内容可直接复制到表格软件。
- [x] 加载、失败重试、无权限、无数据、继续加载和数据截断均有独立反馈。
- [x] 页面样式仅使用全局 Design Token，并适配底部安全区。
- [x] Node 自动化测试覆盖范围继承、旧入口兼容、专属页面跳转、筛选、防抖、分页、导出、隐私脱敏、异常状态和页面注册，共 9 项。
- [ ] 微信开发者工具验证四类真实业务数据、员工/子类型筛选和跨月范围继承。
- [ ] 真机验证长列表加载、大字体布局，以及 CSV 粘贴到 Excel/WPS 后的中文编码和列结构。

## 员工模式入口

- [x] “运营与对账”仅在管理员角色下显示，普通员工不获得账务入口。
- [x] 入口跳转至小程序业务账汇总页，不改变补差价、核销和开票等现有角色路由；已下线的线索入口不再出现。
- [x] 入口说明明确覆盖小程序业务账、核销和长河令汇总。
- [x] Node 自动化测试覆盖管理员可见性、目标路由，并回归开票管理权限。
- [ ] 微信开发者工具分别以管理员、前台、溪降检票和酒吧角色验证入口显隐。

## 营地营业时间

- [x] 业务配置、营业状态兜底、攻略测试夹具和业务参数文档统一为 `09:30–21:00`。
- [x] `09:29` 判定未营业，`09:30` 判定开始营业，`21:00` 仍营业，`21:01` 判定结束。
- [x] `19:00` 停止供餐规则保留，之后营地为部分服务停止但仍在营业。
- [x] 溪降 `10:00–16:30` 停止检票口径保持不变。
- [x] 首页、园区和攻略相关 52 项 Node 自动化测试全部通过。
- [ ] 真机在临界时间或通过调试时钟验证首页与园区页状态文案同步。

## 开票与退款运行环境

- [x] 线上 `invoiceService`、`getMyOrders`、`requestTicketRefund`、`refundMember` 和 `initDb` 均处于部署完成状态。
- [x] 无种子执行线上 `initDb`，确认既有集合保持不变，并创建此前缺失的 `invoice_requests` 集合。
- [x] 缺失 `invoice_requests` 导致的开票加载失败与会员退款 `-504002` 已从运行环境根因上消除。
- [x] 云函数传输失败统一转为用户可理解提示，同时保留错误码、函数名和原始错误供调试使用。
- [x] 申请开票错误态展示具体可恢复提示，点击“重新加载”会重新请求服务。
- [x] `cloudbaserc.json` 和批量部署脚本已补齐开票、退款、继续支付与运营账函数，避免后续环境漏部署。
- [x] 部署脚本的服务端口检测已修复，不再把任意 CLI 异常误判成端口可用。
- [x] 管理员 `adminOperationsLedger` 基础版本已部署到线上环境并复核状态为 Deployment completed。
- [x] 最新 `adminOperationsLedger`、`invoiceService`、`createSelfUpgrade` 和 `seedUpgradeItems` 已重新部署到线上，开发者工具 CLI 复核均存在。
- [x] 相关 25 项 Node 自动化测试通过。
- [ ] 在微信开发者工具使用真实已完成订单打开“申请开票”，确认订单金额和表单正常出现。
- [ ] 在真机使用满足规则的会员卡申请退款，确认不再出现 `-504002`，并核对微信退款结果。

## 待支付订单取消

- [x] 会员卡、门票和补差价三类待支付订单显示“取消订单”，其他类型及非待支付状态不显示。
- [x] 点击取消先展示二次确认，放弃确认不会调用云函数。
- [x] 取消仅将订单更新为 `cancelled`，同时记录取消原因和时间，不删除订单及原始金额。
- [x] 云函数按当前用户 OpenID 和订单号校验归属，并在事务内重读状态，避免覆盖已支付订单。
- [x] 取消成功后自动刷新当前列表；订单从“待支付”移除，并继续保留在“全部”中显示为“已取消”。
- [x] 支付或其他请求抢先改变状态时，展示服务端原因并刷新最新订单状态。
- [x] 部署配置和批量部署脚本已注册 `cancelPendingOrder`。
- [x] `cancelPendingOrder` 已部署到线上环境，`getMyOrders` 已同步新版展示字段，两者均复核为部署完成。
- [x] Node 自动化测试覆盖类型与状态规则、审计字段、归属和事务约束、二次确认及冲突刷新。
- [ ] 微信开发者工具分别用会员卡、门票和补差价待支付订单验证取消后列表变化。
- [ ] 真机验证二次确认、重复点击防护，以及“继续支付”和“取消订单”并排时的小屏布局。
- [ ] 真机模拟支付面板已打开时的取消竞态，确认支付成功回调最终仍以“已完成”为准。

## 本批交付前回归

- [x] `invoiceService` 与 `adminOperationsLedger` 均已写入 `cloudbaserc.json` 和批量部署脚本。
- [x] 数据库文档列明运营报表所需 `paidAt`、`usedAt`、`refundedAt`、`createdAt` 普通索引，以及票券 `orderId` 查询索引。
- [x] 电子发票申请集合继续要求“所有用户不可读写”，文件临时地址只可经云函数鉴权获取。
- [x] 本批 13 个开票、门票与运营数据测试文件共 161 项全部通过。
- [x] 项目现有测试共 504 项全部通过；线索功能下线后对应旧测试已一并移除。
- [x] 相关云函数与页面 JavaScript 语法检查、`git diff --check` 全部通过。
- [ ] 云开发控制台确认上述索引已实际建立，并再次确认发票文件存储权限未公开。
- [x] 微信开发者工具“服务端口”已恢复，最新版 `adminOperationsLedger` 已部署并由 CLI 复核。
- [x] 2026-07-28 已将最新版 `adminOperationsLedger` 与 `createUpgradeCharge` 部署至 `cloud1-d4gzkwy3w150d2fd2`，两项部署结果均为成功。
- [x] 微信开发者工具完成普通编译，“问题”面板为 0；控制台仍有仅指向 `WAServiceMainContext` 的基础库超时信息，未将其作为业务功能通过依据。

## 员工模式工作台

- [x] 已按现场使用频率重排：主任务优先、常用任务其次、管理工具最后，避免所有功能同权重纵向堆叠。
- [x] 四类员工角色使用稳定任务注册表，前台、溪降、酒吧和管理员只看到各自授权入口。
- [x] 管理员首页使用轻量接口加载今日经营、核销、长河令和待处理开票摘要，单个摘要失败不阻断工作入口。
- [x] 身份查询失败、未绑定、已绑定、概览部分失败和重试状态均有独立反馈。
- [x] 页面继承长河令深色视觉体系，并使用全局圆角、间距、字号、阴影和动效 Token。
- [x] 高频主操作满足现场触控尺寸，并提供轻触觉反馈。
- [x] 自动化覆盖角色权限、任务排序、摘要降级、路由、触控尺寸与视觉 Token。
- [ ] 真机分别以四种员工角色验证弱网、室外强光、大字体及连续扫码后的返回路径。

## 补差价升级购买数量

- [x] 7 个升级项目统一支持每单购买 1–10 份，默认 1 份，不再仅双人票支持数量。
- [x] 加减控件在 1 和 10 处正确禁用，当前数量、单价和合计金额同步显示。
- [x] 客户端只提交 `itemId`、数量和可选员工尾号；最终金额始终由云端单价乘数量计算。
- [x] 云端拒绝 0、11、小数和非数字数量，旧客户端未传数量时按 1 份兼容。
- [x] 页面明确提示每份升级需持有一张对应原票，避免多人合并付款后少验原票。
- [x] 支付成功态保留实际购买数量，用户取消支付时保留当前项目、数量和员工信息以便重试。
- [x] 自动化覆盖 7 个项目、上下限、异常输入、服务端金额和旧客户端兼容，共 19 项。
- [ ] 真机验证 1 份、2 份、10 份支付拉起金额及支付成功后的订单展示。

## 员工补差价收款数量

- [x] 选择预设升级项目后显示 1–10 人步进器，默认 1 人。
- [x] 页面同步展示项目单价与“单价 × 人数”的应收合计，二维码承载金额为该合计。
- [x] 云函数重新读取升级项目单价并计算总金额，不接受客户端传入的总价。
- [x] 数量 0、11、小数和非数字均由云端拒绝；旧客户端未传数量时兼容为 1 人。
- [x] 手动输入金额继续表示整单总额，不与预设项目人数叠乘。
- [ ] 真机分别生成 1 人、2 人和 10 人收款码，并由另一台手机扫码核对待支付总额。

## 服务线索功能下线

- [x] 游客端线索表单、我的线索页、员工线索页及对应路由和入口均已删除。
- [x] 4 个线索云函数的本地源码和部署清单引用均已删除。
- [x] 线上 `createServiceLead`、`getMyServiceLeads`、`listAssignedLeads`、`updateServiceLead` 已删除，CLI 复核 4/4 均不存在。
- [x] 门票咨询改为直接进入统一管家联系路径，未留下失效表单兜底。
- [x] 初始化脚本和领域常量不再创建或读写服务线索集合。
- [x] 历史 `service_leads` 数据未删除，满足后续审计或人工导出需要。
- [x] 运行代码残留扫描、退役路径检查与线索下线回归测试全部通过。

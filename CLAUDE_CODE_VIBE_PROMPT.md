# Claude Code 执行提示词：森水长河 Vibe UI／UX 全盘重构

目标仓库：`18523102883hao-boop/senshui-miniprogram`  
目标：保留现有业务底座，完成用户侧信息架构、视觉系统、公共组件与交互体验的分步重构。

---

## 一、你的角色

你是本项目的：

- 高级产品设计师
- 用户体验架构师
- 微信原生小程序前端工程师
- Design System 负责人
- 代码重构与质量审查工程师

不要另起 HTML Demo，不要创建无法合并的展示项目。所有改动必须落到当前微信小程序仓库。

---

## 二、开始前必须完成

1. 检查 Git 状态、当前分支和未提交改动。
2. 不覆盖用户未提交代码，不强制推送，不擅自合并到 `main`。
3. 阅读：
   - `app.json`
   - `app.js`
   - `app.wxss`
   - 全部游客端 `pages/`
   - 全部 `components/`
   - 云函数调用与状态定义
   - `STYLE.md`
   - `ICON_MANIFEST.md`
   - `ICON_USAGE_GUIDE.md`
   - `icon-map.json`
   - `preview/ui-ux-target-board.png`
4. 先输出 `docs/uiux/01-current-audit.md`，列出：
   - 当前路由与页面责任
   - 重复入口
   - 重复 WXSS
   - 图标混用
   - 状态不一致
   - 支付、预约、会员和长河令的改造风险
5. 若没有阻断风险，继续建立设计系统和首页；不要只停留在报告。

---

## 三、业务保护红线

不得擅自改变：

- 微信登录与手机号授权
- 会员价格、有效期和权益
- 长河令余额、发放和流水
- 门票、补差价与套票规则
- 溪降预约、名额和上车号
- 商品、酒水、租赁与押金
- 支付、退款和售后
- 隐私授权、隐私政策和用户协议
- 员工端和云函数数据结构

UI 重构必须复用现有数据层和业务状态。遇到金额、权益或退款规则不清晰时，先读取代码，不自行假设。

---

## 四、目标信息架构

游客端最终采用四个一级 Tab：

1. **向导**：环境状态、今日玩法、地图、活动、当前下一步。
2. **长河令**：余额、悬赏、拍卖、兑换和成就。
3. **行程**：门票、预约、上车号、营位、配送、租赁、退款与历史。
4. **我的**：资料、会员、钱包、优惠券、收藏、设置与合规。

园区地图不占一级 Tab，但在向导首页、到园状态和当前行程中保持高可达性。

若当前项目仍是 3 Tab，不要立即改动业务页面。先独立实现并验证 custom tabBar，再切换路由。

---

## 五、首页必须状态驱动

首页不得永远显示相同模块。

至少处理：

- 首次访问：卖点、开放状态、购票、预约。
- 未购买：套票、活动、攻略和会员。
- 已购票：票码、日期、天气、交通和行前准备。
- 已预约：预约码、集合点、上车号、改签取消。
- 已到园：地图、下一场活动、酒水配送、小卖部、长河令。
- 行程完成：成就、评价、会员与复购。

首页首屏只出现一个主行动；快捷入口最多四个。

---

## 六、视觉执行

严格读取 `STYLE.md`，不得新增随机色值、圆角和阴影。

### 浅色主题

- 背景 `#F6F4F0`
- 主卡 `#FFFFFF`
- 品牌绿 `#244B36`
- 主文字 `#172019`
- 金色 `#D5A43A`
- CTA 珊瑚 `#F07055`

### 长河令深色主题

- 背景 `#101512`
- 卡片 `#18201B`
- 文字 `#F7F3EA`
- 金色仅用于资产、奖励和会员
- 通过内部弥散光和大数字建立层级，不使用花哨边框

不加载外部字体、网络 iconfont、Font Awesome 或 Tailwind。

---

## 七、图标接入

1. 把资源包中的 `assets/icons/` 复制到当前小程序静态资源目录。
2. 路径和用途只读取 `icon-map.json`，不要凭感觉重命名。
3. 浅色页面使用 `forest/`，深色页面使用 `ivory/`。
4. 底部导航使用 `tabbar/`：
   - 浅色页面：普通 `default/active`
   - 长河令页：`dark-default/dark-active`
5. 删除 Emoji、字符图标和网络 iconfont。
6. 不修改原始 256×256 PNG，通过 WXML 尺寸控制显示大小。
7. 若页面已有功能正确但图标不一致，只替换资源，不改业务事件。

推荐封装 `sr-icon` 组件，统一处理名称、主题和尺寸。

---

## 八、必须落地的交互

### 整卡点击

活动、任务、行程、商品、路线和会员权益的整张卡片可点击。取消重复的“查看详情/去完成”按钮。

### 按压反馈

- 大卡片：`scale(.985)`
- 普通入口：`scale(.97)`
- CTA：`scale(.96)`
- 120–160ms
- 使用 `hover-class` 或明确 pressed state，避免状态卡住

### 振动

统一调用 `utils/haptics.js`：

- Tab、日期、场次、收藏：light
- 预约、支付、领取长河令成功：medium
- 风险确认与取消：heavy
- 滚动和普通浏览不振动

### 底部半屏卡片

地图 POI、活动摘要、商品规格、配送营位使用半屏卡；支付、复杂表单、协议使用完整页。

### 横向选择

日期、时段、活动分类、商品分类使用横向 `scroll-view`，选中项自动滚入可见区域。

### 页面动效

只优先动画 `transform` 与 `opacity`：

- 快速反馈 120–160ms
- 页面切换 200–240ms
- 页面进入 280–340ms
- 底部弹层 300–360ms

设置页“减少动态效果”开启时，取消非必要转场。

---

## 九、推荐公共组件

```text
components/ui/
├── sr-icon/
├── sr-card/
├── sr-status-chip/
├── sr-section-header/
├── sr-action-card/
├── sr-trip-card/
├── sr-activity-card/
├── sr-product-card/
├── sr-environment-card/
├── sr-member-card/
├── sr-bottom-sheet/
├── sr-stepper/
├── sr-skeleton/
├── sr-empty-state/
└── sr-error-state/
```

长河令专用：

```text
components/ling/
├── sr-ling-balance/
├── sr-ling-stat/
├── sr-bounty-item/
├── sr-auction-banner/
└── sr-reward-card/
```

组件不直接调用支付和云函数；页面负责业务，组件通过 properties 与 events 接收数据和操作。

---

## 十、实施顺序

1. Git 和代码审计
2. Design Token 与基础样式
3. `sr-icon`、状态标签、卡片、骨架屏和反馈工具
4. custom tabBar 四 Tab 与深浅主题
5. 向导首页高保真重构
6. 截图、自测并暂停确认
7. 长河令
8. 行程中心
9. 地图与溪降预约
10. 商品与服务
11. 我的、会员与设置
12. 全局一致性和真机性能检查

每次只改一个页面或一个公共组件。每完成一阶段：

- 列出修改文件
- 给出新旧截图
- 给出状态测试
- 给出业务影响
- 单独 Git 提交

---

## 十一、首页完成后的暂停条件

首页完成后必须暂停，不要自动继续全部页面。输出：

- 首页结构
- 用户状态判断
- 使用的公共组件
- 接入的图标
- 加载/空/错状态
- 微信开发者工具截图
- 自测结果
- 未完成项

等待确认后再进入长河令和行程中心。

---

## 十二、验收标准

### 产品

- 10 秒内能看到当前状态和下一步。
- 首页不是功能仓库。
- 已购票用户首屏看到行程。
- 到园用户首屏能进入地图。
- 临近预约能看到集合点与上车号。
- 同一状态跨页面文案一致。

### 视觉

- 所有图标来自本资源包。
- 浅色和长河令深色主题明显但统一。
- 圆角、间距、字号、阴影遵循 `STYLE.md`。
- 不滥用玻璃、渐变和阴影。
- 关键文字清晰可读。

### 交互

- 卡片有自然按压态。
- 振动克制且语义一致。
- Tab 切换无闪白和跳动。
- 提交防重复。
- 失败后不丢用户输入。
- 弱网、空数据、无库存和停运路径完整。

### 工程

- 不破坏支付、预约、会员和长河令。
- 不引入网络字体和图标。
- 无控制台错误和资源 404。
- 低端安卓滚动不卡顿。
- 不提交密钥与敏感文件。

现在开始：先审计当前最新代码，然后建立设计系统与首页，不要输出无法执行的空泛建议。

# T05 · 首页向导（活动表 / 地图 / 公告 / 快捷入口）

- 状态：✅ 骨架（需后台联调 + 素材）
- 依赖：T02-04

## 已交付
- 页面：`miniprogram/pages/index/`（公告横幅 / 快捷入口 / 园区地图 / 今日活动）
- 云函数：`cloudfunctions/getHomeData`（读 activities 今日 + notices 公告/地图）
- 无数据时降级为占位内容，不报错。

## 接口契约
`getHomeData()` → `{ notice, mapImage, activities:[{id,time,name,location,rewardLing}] }`

## 待完善
1. **园区地图**：上传高清图到云存储，写入 `notices(type:'config', key:'homeMap', value:fileID)`；V1 用高清图 + 点位说明，不做交互地图。
2. **活动表后台可配**：`activities` 集合按日配置（后台/云开发内容管理），改动 1 分钟内生效（可加缓存失效）。
3. 快捷入口「约溪降」当前为「敬请期待」，溪降上线（T08）后改为跳转预约页。
4. 下拉刷新已接。

## 验收
- [ ] 内容全部后台可配，改动 ≤1 分钟生效
- [ ] 地图点击可查看大图
- [ ] 弱网下占位不白屏

# T06 · 玩·长河令内容页（故事 / 规则 / 擂台 / 兑换预览）

- 状态：✅ 骨架（需后台联调）
- 依赖：T05

## 已交付
- 页面：`miniprogram/pages/ling/`（我的令数 / 江湖故事 / 怎么赚令 / 怎么用令 / NPC 巡游）
- 云函数：`cloudfunctions/getLingContent`（擂台读 activities，故事/兑换/NPC 读 notices）、`getLingBalance`（令余额）

## 接口契约
- `getLingContent()` → `{ story?, earnList?:[{id,name,reward,join,time,location}], exchangeList?, npcSchedule? }`
- `getLingBalance()` → `{ balance }`

## 合规（生死线，务必遵守 `_conventions.md` §6）
- 只用「奖励令数 / 参与令数」；禁用押注/下注/翻倍/赌等词。
- 兑换/拍卖为**预览展示**，V1 不做线上兑换/竞拍（页面已标注「秋季开放」）。
- 长河令不线上直售、不兑现金。

## 待完善
1. 擂台/兑换表接后台 `activities(category:'ling')` 与 `notices(type:'ling_config')`。
2. 上线前跑敏感词扫描（T17）。

## 验收
- [ ] 文案通过合规词扫描 0 命中
- [ ] 我的令数与 ling_accounts 一致
- [ ] 擂台列表后台可配

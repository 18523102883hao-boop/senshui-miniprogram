# T02 · 云开发初始化（环境 / 集合 / 权限）

- 状态：✅ 已实现
- 依赖：T01

## 已交付
- `cloudfunctions/initDb`：一键建全部集合 + 可选种子数据。
- `scripts/db-init.md`：集合清单 + 索引清单 + 权限建议 + 支付配置。
- `docs/数据模型.md`：全集合字段定义。

## 待人工操作
1. 新建云环境，环境 ID 写入 `miniprogram/env.js`。
2. 上传部署 `initDb`，云端测试入参 `{ "seed": true }`。
3. 按 `scripts/db-init.md` 建**唯一索引**（`users._openid`、`members.memberCode`、`orders.outTradeNo` 等）。
4. 集合权限设为「所有用户不可读写」，读写全部经云函数。

## 验收
- [ ] 14 个集合建齐
- [ ] 唯一索引建立（防重复发卡第二道保险）
- [ ] 任一云函数可正常调用返回 `{code:0}`

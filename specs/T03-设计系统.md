# T03 · 设计系统（主题皮肤 + 组件规范）

- 状态：✅ 已实现
- 依赖：无

## 已交付
`miniprogram/app.wxss` 定义了「长河令江湖」视觉 token（CSS 变量，穿透组件样式隔离）与通用类。

## 设计 Token
| 类别 | 变量 | 值 |
|---|---|---|
| 背景 | --sr-bg / --sr-bg-card | #F5F1E9 / #FAF6EE |
| 主色（墨绿） | --sr-primary / --sr-primary-light | #2A4D3A / #3A6B4F |
| 强调（朱砂印章） | --sr-seal | #B23A2E |
| 辅助（铜金） | --sr-gold | #B8860B |
| 文字 | --sr-text / --sr-text-sub | #2B2B2B / #6B6B6B |
| 圆角/阴影 | --sr-radius / --sr-shadow | 20rpx / 墨绿柔阴影 |

## 通用类
`.sr-page` `.sr-card` `.sr-btn`(`--seal`/`--ghost`) `.sr-title` `.sr-subtitle` `.sr-badge` `.sr-divider` `.sr-row-between` 等。

## 完善建议（可选）
- 引入 TDesign（`miniprogram/package.json` 已声明依赖），复杂交互（弹窗/表单/Toast）用 TDesign 组件并覆盖主题色。
- 补充朱砂印章图标、江湖字体标题图（首页/长河令 banner）。

## 验收
- [ ] 5 个示例页视觉统一
- [ ] 主题色集中可改（改变量即全局生效）

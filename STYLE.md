# 森水长河小程序 Vibe UI／UX 视觉交互规范 v2.0

## 1. 产品定位

森水长河小程序不是景区功能目录，也不是普通商城。它是围绕游客完整旅程运行的数字游园助手：

`了解园区 → 购票/预约 → 行前准备 → 到园核验 → 地图与游玩 → 商品服务 → 长河令 → 售后与复购`

本轮执行原则：

> 产品体验全盘规划，业务代码分步重构。  
> 先 UX，后 UI。  
> 不推翻已经稳定的支付、预约、会员、长河令和云函数逻辑。

## 2. 一级信息架构

游客端建议采用四个一级 Tab：

1. **向导**：今天怎么玩、环境状态、地图、活动、下一步行动。
2. **长河令**：资产、任务、拍卖、兑换、成就。
3. **行程**：门票、预约、上车号、营位、配送、租赁、售后。
4. **我的**：身份、会员、钱包、优惠券、收藏、设置和合规。

首页不得继续堆叠九宫格。首页内容必须根据用户状态变化：

- 首次访问：卖点、开放状态、购票与预约。
- 已购票：票码、日期、天气、交通与行前准备。
- 已预约：预约码、集合点、上车号、改签取消。
- 已到园：地图、下一场活动、商品服务、长河令。
- 行程完成：成就、评价、会员与复购。

## 3. 视觉气质

关键词：

- 自然探索
- 森林水岸
- 克制留白
- 轻奢户外
- 现场行动
- 沉浸游戏

参考优秀产品的层级、触觉和动效哲学，但不能复制其品牌视觉。

禁止：

- 满屏白色圆角卡片
- 所有模块都使用渐变
- 所有卡片都使用毛玻璃
- 每一行都放“查看详情/去完成”
- Emoji 与多套图标混用
- 廉价电商红
- 为了高级感降低文字对比度

## 4. 设计令牌

### 浅色主题

| Token | 值 |
|---|---|
| 页面背景 | `#F6F4F0` |
| 主要表面 | `#FFFFFF` |
| 柔和表面 | `#EEEDE7` |
| 主文字 | `#172019` |
| 次文字 | `#667069` |
| 品牌绿 | `#244B36` |
| 深森林绿 | `#173628` |
| 河川绿 | `#4F7963` |
| 山野金 | `#D5A43A` |
| 行动珊瑚 | `#F07055` |
| 分隔线 | `rgba(36,75,54,.10)` |

### 长河令深色主题

| Token | 值 |
|---|---|
| 页面背景 | `#101512` |
| 卡片背景 | `#18201B` |
| 次级卡片 | `#202A23` |
| 主文字 | `#F7F3EA` |
| 次文字 | `rgba(247,243,234,.62)` |
| 分隔线 | `rgba(255,255,255,.08)` |

金色只用于长河令、奖励、会员和关键资产；珊瑚色只用于交易 CTA、错误和强提醒。

## 5. 字体

不加载网络字体。

```css
font-family:
  -apple-system,
  BlinkMacSystemFont,
  "SF Pro Display",
  "PingFang SC",
  "Helvetica Neue",
  Arial,
  sans-serif;
```

数字使用系统几何字体与等宽数字：

```css
font-variant-numeric: tabular-nums;
```

建议层级：

- 核心数字：72–104rpx / 800–900
- 页面标题：44–52rpx / 700
- 区块标题：34–38rpx / 650–700
- 卡片标题：30–32rpx
- 正文：28rpx
- 辅助：24rpx
- 标签：22rpx

## 6. 间距与圆角

8rpx 基础网格：

`8 / 16 / 24 / 32 / 40 / 48 / 64rpx`

- 页面左右：32rpx
- 普通卡片内边距：32rpx
- 重点卡片内边距：40rpx
- 模块间距：40–48rpx
- 小控件圆角：20rpx
- 状态标签：24rpx
- 普通卡片：32rpx
- 主要卡片：48rpx
- 沉浸资产卡：56–64rpx

## 7. 交互原则

### 整卡触摸

活动、行程、任务、商品、路线、会员权益的整个卡片均为触摸区域。除支付、确认、取消、退款和授权外，减少独立按钮。

### 按压反馈

- 大卡片：`scale(.985)`，120–160ms。
- 普通入口：`scale(.97)`。
- 主 CTA：`scale(.96)`。
- 不对大面积卡片使用 `scale(.94)`，避免页面抖动。

### 触觉反馈

- Tab、日期、场次、收藏：轻振动。
- 预约成功、支付成功、领取长河令：中等振动。
- 风险确认、取消：较强振动。
- 普通滚动、浏览、输入：不振动。
- 振动失败不得阻断业务。

### 页面动效

- 快速反馈：120–160ms
- 普通切换：200–240ms
- 页面进入：280–340ms
- 底部弹层：300–360ms

只使用 `transform` 和 `opacity` 作为主动画属性。

### 半屏弹层

地图 POI、活动摘要、商品规格、配送营位和日期人数选择使用底部半屏卡片；长协议、复杂表单和支付流程使用完整页面。

## 8. 页面规范

### 向导

首屏只回答：

- 今天是否开放
- 天气、水位和水质
- 我当前的票或预约
- 下一步行动
- 最近活动

快捷入口最多四个，第五个统一为“全部服务”。

### 长河令

默认深色；余额卡依靠超大数字与内部弥散光建立层级，不堆边框。任务整行触摸；奖励和拍卖使用金色；过期和风险使用珊瑚色。

### 行程

按照“当前行程、待出行、进行中、售后、历史”组织，不按后台订单类型拆散。票务、预约、接驳、营位、配送与租赁尽量聚合在同一行程中。

### 地图

地图占主视野；点击 POI 先弹出底部半屏卡片，再进入详情。定位点有轻微呼吸动画，但不得高频重绘。

### 预约

每个页面只做一个主要决策。日期横滑、时段单选、项目卡单选，底部价格实时更新。支付和提交必须防重复。

### 商品与服务

酒吧、小卖部、装备租赁、免费借用和长河令兑换按场景分组。购物车固定展示配送营位、数量和结算。

### 我的

首屏展示身份、会员、钱包和行程；设置和协议放在后半屏。9.9会员卡使用深绿与极淡金色，不做红色促销卡。

## 9. 图标

- 浅色页面：`assets/icons/forest/`
- 深色页面：`assets/icons/ivory/`
- Tab：`assets/icons/tabbar/`
- 状态：`assets/icons/status/`
- `icon-map.json` 是唯一映射表。
- 同一页面不混用 Emoji、系统字符、网络 iconfont 与本资源包。
- 普通图标 36–48rpx，快捷入口 48–56rpx，Tab 48–56rpx。

## 10. 性能与无障碍

- 主触摸区不小于约 88rpx。
- 状态不能只靠颜色表达。
- 适配安全区、刘海屏和大字号。
- 设置页提供“减少动态效果”。
- 低端安卓滚动列表内不使用大量实时 blur 与重阴影。
- 加载使用结构化骨架屏；失败保留已有数据并提供重试。
